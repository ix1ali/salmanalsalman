# تشغيل النظام على salmanalsalman.app

الخطوات مرتّبة. اتبعها بالترتيب مرة واحدة فقط، ثم لن تحتاج إليها بعدها.
الوقت المتوقّع: ١٥ دقيقة.

---

## ١) قاعدة البيانات — Supabase

١. ادخل [supabase.com](https://supabase.com) → **New project**
   - الاسم: `salmanalsalman`
   - المنطقة (Region): **Central EU (Frankfurt)** أو أقربها للكويت
   - **Database Password**: اكتب كلمة مرور قوية واحفظها عندك (تُستعمل للطوارئ فقط)

٢. انتظر حتى يجهز المشروع، ثم افتح **SQL Editor** → **New query**

٣. افتح ملف `supabase/schema.sql` من هذا المستودع، انسخ **كل** محتواه، الصقه في المحرّر، واضغط **Run**.

   يُنشئ هذا الملف:
   - كل الجداول (العقارات، الأدوار، الشقق، المستأجرون، العقود، الوصولات، المصروفات، المستندات، المراسلات، **سجل العمليات**)
   - الصلاحيات داخل قاعدة البيانات نفسها (RLS)
   - التحديث اللحظي بين المستخدمين
   - مخزن الملفات الخاص للمستندات
   - **حسابك**: `Ali` / `Aa112233@`

٤. **Authentication → Sign In / Providers → Email**:
   - أوقف **Confirm email** (لا يوجد بريد حقيقي، الدخول باسم المستخدم فقط)
   - أوقف **Allow new users to sign up** — الحسابات تُنشأ من داخل النظام بيد المدير فقط

٥. **Project Settings → API** — انسخ من هناك:
   - `Project URL`
   - `anon` `public` key

> المفتاح `service_role` **لا يُستعمل ولا يُنسخ إلى أي مكان**. النظام لا يحتاجه.

---

## ٢) النشر — Vercel

١. المشروع مربوط أصلًا بـ [vercel.com/hayakel/salmanalsalman](https://vercel.com/hayakel/salmanalsalman).
   تأكّد في **Settings → Git** أنه مربوط بمستودع `ix1ali/salmanalsalman` وأن **Root Directory** = `aqar`.

٢. **Settings → Environment Variables** — أضف المتغيرين التاليين لكل البيئات
   (Production و Preview و Development):

   | الاسم | القيمة |
   |---|---|
   | `NEXT_PUBLIC_SUPABASE_URL` | `Project URL` من الخطوة ١-٥ |
   | `NEXT_PUBLIC_SUPABASE_ANON_KEY` | `anon public` من الخطوة ١-٥ |

٣. **Deployments → أحدث نشر → Redeploy** حتى تُقرأ المفاتيح الجديدة.

> ما دامت هذه المفاتيح غير مضبوطة، يعمل النظام محليًا داخل المتصفح فقط
> (وضع تجربة، بلا مشاركة). بمجرد ضبطها يتحوّل تلقائيًا إلى الوضع المشترك.

---

## ٣) الدومين — salmanalsalman.app

**Settings → Domains → Add** ثم اكتب `salmanalsalman.app`.

- إن كنت اشتريت الدومين من Vercel فسيُربط تلقائيًا خلال دقائق.
- إن كان من جهة أخرى، أضف السجلات التي يعرضها Vercel عند مزوّد الدومين:
  - `A` للجذر `@` → `76.76.21.21`
  - `CNAME` لـ `www` → `cname.vercel-dns.com`

أضف `www.salmanalsalman.app` أيضًا واجعله يحوّل إلى الجذر.

---

## ٤) أول دخول

افتح `https://salmanalsalman.app` وسجّل الدخول:

| | |
|---|---|
| **اسم المستخدم** | `Ali` |
| **كلمة المرور** | `Aa112233@` |

اسم المستخدم **لا يفرّق بين الحروف الكبيرة والصغيرة** — `Ali` و`ali` و`ALI` كلها تعمل.

ثم:

١. **الإعدادات → الاتصال والمشاركة → «تهيئة بيانات المكتب»**
   يرفع سجل عمارة تراب كاملًا (٩٢ وحدة، ٥٩ مستأجرًا وعقودهم) إلى قاعدة البيانات.
   يظهر هذا الزر مرة واحدة فقط، ما دامت القاعدة فارغة.

٢. **الإعدادات → حسابي → تغيير كلمة المرور** — غيّر `Aa112233@` إلى كلمة مرور تخصّك.

---

## ٥) إضافة بقية المستخدمين

**المستخدمون → + مستخدم**. اختر الاسم والدور وكلمة المرور، والحساب يصير جاهزًا فورًا.

| الدور | ماذا يستطيع |
|---|---|
| **مدير** | كل شيء: التعديل، المالية، الوصولات، المستخدمون |
| **محاسب** | اطلاع كامل على كل شيء بما فيه المالية، بلا أي تعديل |
| **حارس** | الشقق والمستأجرون والتنبيهات والمراسلات — **بلا أي بيانات مالية** |

اسم المستخدم لا يمكن تغييره بعد الإنشاء لأنه هوية الحساب. أما الاسم الظاهر والدور
وكلمة المرور فتُغيَّر متى شئت.

---

## ما الذي يعنيه «مشترك ولحظي»

- كل تعديل يحفظه أي مستخدم يظهر عند البقية **خلال ثانية** دون إعادة تحميل الصفحة.
- كل إضافة أو تعديل أو حذف تُقيَّد في **سجل العمليات** باسم فاعلها ووقتها بالضبط:
  **المستخدمون → السجل**.
- إن انقطع الاتصال أثناء الحفظ يظهر تنبيه أحمر في الشريط العلوي، ويُعاد تحميل
  الحالة الصحيحة من الخادم — فلا يبقى تعديل معلّق في الهواء دون أن تدري.

---

## الأمان

| | |
|---|---|
| كلمات المرور | مجزّأة بـ bcrypt على الخادم، لا تُخزَّن ولا تُنقل كنص |
| الصلاحيات | مطبَّقة داخل قاعدة البيانات نفسها (RLS) لا في الواجهة فقط — الحارس لا يستطيع قراءة المالية حتى لو حاول تجاوز الواجهة |
| المستندات | مخزن خاص غير علني، وروابط العرض مؤقّتة تنتهي خلال ١٠ دقائق |
| الاتصال | TLS إجباري + سياسة أمن محتوى تمنع أي سكربت أو اتصال بجهة خارجية |
| المحاولات الفاشلة | إيقاف ٩٠ ثانية بعد ٥ محاولات، فوق حماية الخادم نفسه |
| الخمول | خروج تلقائي بعد ١٢ ساعة بلا نشاط |
| إنشاء الحسابات | التسجيل الذاتي مغلق — لا حساب إلا بيد المدير |

---

## نسخة احتياطية

**الإعدادات → البيانات → تصدير نسخة** ينزّل ملف JSON بكل السجلات.
احتفظ بنسخة كل فترة. Supabase أيضًا يأخذ نسخًا يومية تلقائية.

---

## للتشغيل على جهازك أثناء التطوير

```bash
cd aqar
npm install
cp .env.example .env.local   # ثم املأ المفاتيح، أو اتركها فارغة للوضع المحلي
npm run dev
```
